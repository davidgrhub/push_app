version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: mysql_push_app
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: wjQkRHPTvVtl
      MYSQL_DATABASE: push_automation_db
      MYSQL_USER: david
      MYSQL_PASSWORD: 1234
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: